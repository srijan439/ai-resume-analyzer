apiVersion: v1
kind: Service

metadata:
  name: ai-resume-service

spec:
  type: NodePort

  selector:
    app: ai-resume-app

  ports:
    - protocol: TCP
      port: 8501
      targetPort: 8501
      nodePort: 30007